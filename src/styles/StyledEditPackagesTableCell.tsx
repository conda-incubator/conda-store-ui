import TableCell from "@mui/material/TableCell";
import { styled } from "@mui/system";

export const StyledEditPackagesTableCell = styled(TableCell)(({ theme }) => ({
  padding: "0px",
  borderBottom: "0px"
}));
