import Typography from "@mui/material/Typography";
import { styled } from "@mui/system";

export const StyledMetadataItem = styled(Typography)(({ theme }) => ({
  padding: "0 0 10px 0",
  fontSize: "13px",
  fontFamily: '"Inter", sans-serif',
  color: "#333"
}));
